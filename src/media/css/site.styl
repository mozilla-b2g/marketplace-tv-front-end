/**
 * Site-wide styles.
 */

@import 'lib';

* {
    box-sizing: border-box;
}

html,
body {
    height: 100%;
}

html {
    font-family: $primary-italic-font-family;
    font-size: 10px;
}

body {
    overflow: hidden;
}

ul {
    margin: 0;
    padding: 0;
}

.hidden {
    display: none;
}

.clearfix {
    &::before,
    &::after {
        content: '';
        display: table;
    }

    &::after {
        clear: both;
    }
}

main {
    height: 100%;
    padding-bottom: 8rem;

    background-color: $main-background-color;
}

.page {
    position: relative;

    height: 100%;
}
