/**
 * Site-wide styles.
 */

@import 'lib';

* {
    box-sizing: border-box;
}

body {
    background-color: $greyscale-grey;
}

ul {
    margin: 0;
    padding: 0;
}

.clearfix {
    &::before,
    &::after {
        content: "";
        display: table;
    }

    &::after {
        clear: both;
    }
}
